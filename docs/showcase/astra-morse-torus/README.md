# Morse Theory: Four Critical Moments

A rising plane reveals the topology of an upright torus: disk, annulus,
punctured torus, and closed torus. Four isolated critical points explain
the changes and the relation $\chi(T^2)=1-2+1=0$.

[Full movie](../assets/astra-morse-torus.mp4) ·
[Preview GIF](../assets/astra-morse-torus.gif) ·
[Manim source](../../../examples/astra/morse_torus.py) ·
[Production record](production.json)

The finished silent movie is 720p at 30 fps. GPT-6 Astra authored the scene
through Codex login. Real TypeSafe Jev evaluated earlier checkpoints, but the
user stopped further model reviews to control costs. The final candidate was
rendered locally and its segments joined without further model/API calls.
The final film is **not Jev-approved**; the production record preserves the
earlier verdicts and marks the final render `not_reviewed`.

Validation: source screening and scene runtime assertions passed; FFprobe
confirmed delivery dimensions, frame rate, and duration; FFmpeg decoded the
entire stitched MP4 without errors. The contact sheet samples the actual movie.
